<h1>Listing Submissions</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @submissions.each do |submission| %>
      <tr>
        <td><%= submission.test.title %></td>
        <td><%= link_to 'Continue', edit_test_submission_path(submission.test, submission) %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

