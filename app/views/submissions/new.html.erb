<h1>New Submission</h1>

<%= form_for [@test,@submission] do |f| %>
    <%= render "tests/show" %>
    <div class="actions">
      <%= f.submit "Start Test" %>
    </div>
<% end %>

<%= link_to 'Back', @test %>
